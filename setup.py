from setuptools import setup, find_packages

version = '0.1'

setup(name='factory_sqlachemy',
      version=version,
      description="",
      long_description="""\
""",
      classifiers=[],
      keywords='',
      author='',
      author_email='',
      url='',
      license='',
      packages=find_packages(exclude=['ez_setup', 'examples', 'tests']),
      include_package_data=True,
      zip_safe=False,
      install_requires=[
          'sqlalchemy'
      ],
      entry_points="""
      # -*- Entry points: -*-
      """,
      )
